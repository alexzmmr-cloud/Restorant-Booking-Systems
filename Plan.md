# Plan.md: Verde Marea Booking System — MVP

## 1. Текущий статус

- Приоритет: `P0`
- Текущее состояние: документы этапа 1 составлены, реализация не начата
- Основная цель цикла: рабочая система бронирования столов с локальным запуском, готовая к приёмке по всем 7 этапам курсового ТЗ
- Обязательное ограничение: локальный запуск, без внешних интеграций, без публичного деплоя, без лишнего production-контура
- Основной контекст проекта: `specification.md`, `design.md`
- Секреты: `DATABASE_URL` и любые токены — только в `.env` (не в `.env.example`, не в коммитах, не в ответах ИИ)
- Сейчас в работе: шаг 1 «Легенда, роли и план»

## 1.1 Целевой сценарий

Гость проходит основной маршрут:

1. Регистрируется, входит в систему
2. Выбирает дату, время, количество гостей — видит только допустимые окна
3. Создаёт бронирование (статус `pending`, стол назначается автоматически)
4. Открывает «Мои бронирования» и уведомления
5. Отменяет бронирование сам, если до начала больше 3 часов

Администратор (admin/super_admin) проходит второй маршрут:

1. Открывает административный контур
2. Фильтрует список бронирований по статусу и дате
3. Подтверждает / отменяет / завершает бронирование, отмечает no_show
4. Управляет столами и правилами доступности окон
5. (только super_admin) меняет роль пользователя user ↔ admin
6. Проверяет историю изменений (AuditLog)

## 1.2 Что не входит в текущую версию

- Внешние интеграции: платежи, email/SMS-провайдеры, OAuth/соцсети
- Публичный деплой и production-инфраструктура
- Мобильное приложение
- Мультитенантность (несколько ресторанов)
- Внешние уведомления — только внутренние, в интерфейсе

## 1.3 Допущения и открытые вопросы

### Допущения

- «Внутренние уведомления» — записи в БД, отображаемые в интерфейсе, без внешней рассылки
- Пересечение окон для стола проверяется по точному совпадению даты+времени слота (слоты дискретны, шаг 90 минут)
- Дедлайн отмены — 3 часа от текущего момента до `date`+`time` бронирования
- Деактивация стола не отменяет уже существующие бронирования, только исключает стол из будущего подбора

### Вопросы, которые нельзя восстановить из входа

- Нужна ли пагинация административного списка бронирований при большом объёме — для MVP допущение: без пагинации, с фильтрами
- Поведение при деактивации стола с активным бронированием — для MVP допущение: бронирование сохраняется как есть

## 2. Требования к результату

- Все сущности и правила из `specification.md` реализованы без расхождений
- Роль проверяется на сервере при каждом чувствительном действии, а не только в интерфейсе
- passwordHash никогда не покидает сервер в ответах API
- Правила доступности окон считаются динамически, не хранятся статичным списком
- Визуальная система Verde Marea (`design.md`) применена одинаково в гостевом и административном контуре
- Обязательные артефакты присутствуют: `specification.md`, `Plan.md`, `AGENTS.md`, `design.md`, `.env.example`, `README.md`/runbook, чек-лист приёмки, журнал проверок
- Ничего сверх зафиксированного в specification.md и этом плане не добавляется без обновления обоих документов

## 2.1 Раскладка по слоям

### Frontend
- Next.js App Router, React, TypeScript, Tailwind CSS, Zod
- Формы: регистрация, вход, бронирование (дата/время/гости)
- Личный кабинет гостя: список бронирований, уведомления, кнопка отмены
- Административный контур: список бронирований с фильтрами, управление столами, управление правилами окон, управление ролями (только super_admin), просмотр истории изменений
- Общие UI-токены и компоненты по `design.md` (кнопки, карточки, бейджи статусов, таблицы, формы, состояния loading/empty/error)

### Backend
- Next.js route handlers / server actions, TypeScript, Zod (те же схемы валидации, что и на клиенте)
- Сессии — упрощённый серверный механизм (без внешних auth-провайдеров)
- Проверка роли на сервере перед каждым чувствительным действием
- bcryptjs: `hash(password, 12)` при регистрации, `compare(inputPassword, user.passwordHash)` при входе
- Логика расчёта доступных окон, подбора стола, переходов статусов, проверки дублей — на сервере

### Database
- `User`: id, email, passwordHash, name, role, createdAt, updatedAt
- `Table`: id, name, capacity, isActive
- `Booking`: id, userId, tableId, date, time, guestsCount, status, createdAt, updatedAt
- `Notification`: id, userId, bookingId, message, isRead, createdAt
- `AuditLog`: id, actorId, action, targetType, targetId, metadata, createdAt

- роли (`User.role`): `user`, `admin`, `super_admin`
- статусы (`Booking.status`): `pending`, `confirmed`, `cancelled`, `completed`, `no_show`

- связи: `User 1—N Booking`, `User 1—N Notification`, `Table 1—N Booking`, `User 1—N AuditLog` (как actor)

- правило доступности окна: ближайшие 14 дней, вторник–воскресенье, шаг 90 минут, диапазон 12:00–21:00, без прошедшего времени, без занятых окон, только при наличии подходящего свободного стола
- правило подбора стола: минимально подходящая вместимость среди свободных на выбранное окно
- правило дублей: один пользователь — не больше одного активного (`pending`/`confirmed`) бронирования на дату+время; один стол — не больше одного бронирования на дату+время
- правило отмены: гость отменяет сам не позднее чем за 3 часа до `date`+`time`; после дедлайна — только admin/super_admin
- правило ролей: admin не может менять роль `super_admin` и не может назначать/снимать `admin`; только `super_admin` меняет `user` ↔ `admin`

## 3. Что делаем по шагам

- [x] 1. Легенда, роли и план
  - Вход: ТЗ курса, палитра и шрифты бренда от пользователя
  - Действие: собраны `specification.md`, `design.md`, `Plan.md`, `AGENTS.md`
  - Результат: зафиксированы легенда, MVP-граница, роли, сценарии, визуальное направление, план по слоям
  - Проверка: ревью через субагента reviewer
  - Итог: документы этапа 1 составлены, ждут ревью перед стартом реализации

- [x] 2. Инициализация проекта и окружения
  - Вход: `specification.md`, `Plan.md`, `design.md` готовы
  - Действие: `create-next-app` с TypeScript+Tailwind, установка Prisma 7 (postgresql), @prisma/client, Zod, bcryptjs, dotenv с точной фиксацией версий; `.env.example` создан и коммитится, реальный `.env` в `.gitignore`
  - Результат: Next.js проект собирается, Prisma-схема валидна, наши документы не задеты генератором
  - Проверка: `npm run build` — чисто, `npx prisma validate` — схема валидна, ревью reviewer
  - Итог: ПРИНЯТО (PASS) — сборка и схема валидны, версии зафиксированы, `.env`/`.env.example` разведены корректно

- [x] 3. Схема данных и миграции
  - Вход: сущности из specification.md/Plan.md раздела Database
  - Действие: `prisma/schema.prisma` с моделями User, Table, Booking, Notification, AuditLog, enum'ами Role и BookingStatus; создана роль/база PostgreSQL `verde_marea`; применена первая миграция `init`
  - Результат: локальная PostgreSQL база разворачивается миграцией без ручных правок, Prisma Client сгенерирован, `npm run build` проходит
  - Проверка: `npx prisma migrate dev` применился чисто на пустой БД, `\dt` показывает все 5 таблиц, ревью reviewer
  - Итог: ПРИНЯТО (PASS) — схема соответствует specification.md без расхождений; первая итерация ревью нашла реальные креды БД в `.env.example`, исправлено плейсхолдером и сменой пароля роли, повторное ревью подтвердило PASS

- [x] 4. Seed и контрольные данные
  - Вход: применённая схема
  - Действие: `prisma/seed.ts` создаёт super_admin, admin, двух гостей (пароль у всех `Password123!`, хеш через bcryptjs 12 rounds) и 6 столов вместимостью 2/2/4/4/6/8; добавлен `lib/prisma.ts` — общий Prisma Client с driver adapter `@prisma/adapter-pg` (обязателен в Prisma 7); добавлено `@unique` на `Table.name`, пересоздана чистая init-миграция (БД была пустой, публикации ещё не было)
  - Результат: `npx prisma db seed` создаёт контрольные данные, повторный запуск не плодит дубли (upsert по email/name)
  - Проверка: `npx prisma db seed` дважды подряд — количество записей не меняется (4 User, 6 Table); `npm run build` проходит; ревью reviewer
  - Итог: ПРИНЯТО (PASS) — seed соответствует specification.md, bcryptjs 12 rounds подтверждён, идемпотентность и сборка проверены практическими прогонами

- [x] 5. Регистрация, вход, сессия
  - Вход: модель User готова
  - Действие: `lib/auth/session.ts` — подписанная HMAC httpOnly-кука (userId + подпись на `SESSION_SECRET`, timing-safe сравнение); `lib/auth/actions.ts` — server actions `registerAction`/`loginAction`/`logoutAction` с bcryptjs.hash(password,12)/compare; `lib/auth/current-user.ts` — роль читается из БД по userId при каждом запросе (не хранится в куке, чтобы смена роли super_admin применялась мгновенно); `lib/auth/require-role.ts` — заготовка проверки роли для будущих защищённых маршрутов; страницы `/register`, `/login`, обновлена `/`
  - Результат: пользователь регистрируется и входит, сессия переживает перезагрузку страницы, роль всегда актуальна из БД
  - Проверка: временный debug-эндпоинт прогнал сценарии (регистрация → bcrypt-хеш корректного формата → сессия установлена → дубль email отклонён → верный пароль входит → неверный пароль отклонён → несуществующий email отклонён с тем же сообщением, что и неверный пароль → прямой bcryptjs.compare подтверждает хеш), затем эндпоинт удалён; `npm run build` проходит; ревью reviewer
  - Итог: ПРИНЯТО (PASS) — bcryptjs 12 rounds дословно по specification.md, passwordHash нигде не покидает сервер (подтверждено grep), защита от user enumeration на месте

- [ ] 6. Серверная проверка ролей
  - Вход: сессии работают, `requireUser`/`requireRole` готовы как переиспользуемая заготовка (шаг 5)
  - Действие: перенесено — проверяется по факту на реальных эндпоинтах вместе с шагами 7-8 (бронирование: `requireUser` на создание/отмену) и 10-11 (админка: `requireRole(['admin','super_admin'])`, `requireRole(['super_admin'])` на смену ролей), а не на пустых заглушках
  - Результат: запрещённые действия блокируются на сервере независимо от интерфейса
  - Проверка: прямой запрос к административному эндпоинту от имени user отклоняется — проверяется в рамках шагов 10-11
  - Итог: перенесено на шаги 7-8 и 10-11 по решению пользователя — рано делать заглушки без реальных действий

- [x] 7. Расчёт доступных окон и подбор стола
  - Вход: модели Table, Booking готовы
  - Действие: `lib/booking/rules.ts` — чистые функции правил (14 дней вт-вс, шаг 90 мин 12:00-21:00, дедлайн отмены 3ч); все "date-only" значения строятся через UTC-полночь (`Date.UTC`), не локальную полночь — иначе `@db.Date` в Prisma съезжает на соседний день из-за смещения таймзоны при сериализации; `lib/booking/availability.ts` — `listAvailableDates`, `listAvailableTimesForDate` (фильтрует прошедшее время и слоты без свободного стола нужной вместимости), `findAvailableTable` (минимальная подходящая вместимость среди свободных)
  - Результат: список доступных окон всегда соответствует правилам и текущим бронированиям, стол подбирается автоматически
  - Проверка: юнит-тест чистых функций (7 слотов/день, 12 из 14 дней после фильтра понедельника, прошедшее время отсекается, дедлайн 3ч работает на границе); интеграционный тест против реальной БД — после бронирования всех столов нужной вместимости на слот, слот пропадает из списка и `findAvailableTable` возвращает null; `npm run build` проходит; ревью reviewer
  - Итог: ПРИНЯТО С ЗАМЕЧАНИЯМИ (PASS WITH NOTES) — UTC-подход к датам подтверждён физически (устраняет реальный риск съезда `@db.Date` на соседний день). Замечание не блокер, но обязательно к учёту на шаге 8: `listAvailableTimesForDate`/`findAvailableTable` не атомарны — защиту от двойного бронирования стола/пользователя нужно закрыть транзакцией и/или частичным unique-constraint при создании брони, а не полагаться только на этот расчёт

- [x] 8. Создание и отмена бронирования гостем
  - Вход: расчёт окон и подбор стола готовы (шаг 7, включая замечание про race condition)
  - Действие: миграция `booking_partial_unique_active` — два частичных UNIQUE-индекса на `(tableId,date,time)` и `(userId,date,time)` только для статусов `pending`/`confirmed` (raw SQL, Prisma schema не выражает частичные индексы напрямую) — это финальный рубеж защиты от гонки, которую не могли закрыть только проверки в коде; `lib/booking/actions.ts` — `createBookingAction` (Zod-валидация → requireUser → проверка прошедшего времени → подбор стола → create, с перехватом Prisma P2002 в понятную ошибку) и `cancelBookingAction` (владение бронированием → допустимый статус → дедлайн 3ч); `app/book/page.tsx`+`booking-form.tsx` — форма дата/гости/время с live-подгрузкой доступности; `app/bookings/page.tsx`+`cancel-button.tsx` — список бронирований с кнопкой отмены
  - Результат: гость создаёт и отменяет бронирования по правилам specification.md; race condition закрыта на уровне БД, не только проверкой в коде
  - Проверка: debug-эндпоинты (созданы и удалены) прогнали — дубль на слот отклонён, прошедшая дата отклонена, отмена заранее работает, повторная отмена и отмена после дедлайна отклонены; **5 параллельных запросов на бронирование единственного свободного стола дали ровно 1 успех** (проверка гонки); сквозной ручной прогон через seed-аккаунт guest1 (вход → `/book` → `/bookings`) подтвердил рабочий flow; `npm run build` проходит
  - Итог: ПРИНЯТО С ЗАМЕЧАНИЯМИ (PASS WITH NOTES) — ревьюер не поверил заявлению про race condition на слово и провёл собственный стресс-тест с искусственной задержкой (форсировал настоящую гонку): 8 параллельных запросов на единственный слот дали ровно 1 успех именно через перехват P2002 от БД-constraint, не через логику кода; отдельно проверил IDOR на cancelBookingAction — отклонён. Найден и исправлен баг — `/bookings`/`/book` без сессии падали с 500 вместо редиректа (`requireUser()` кидал исключение прямо в Server Component); добавлены `requireUserOrRedirect`/`requireRoleOrRedirect` для страниц. Замечание ревью (не блокер) — отсутствие try/catch в клиентских формах на случай истечения сессии между рендером и сабмитом — исправлено сразу: `booking-form.tsx`/`cancel-button.tsx` теперь ловят исключение и редиректят на `/login`

- [x] 9. Личный кабинет гостя и уведомления
  - Вход: бронирования создаются (шаг 8), страница «Мои бронирования» уже готова
  - Действие: `createBookingAction`/`cancelBookingAction` создают `Notification` в той же `prisma.$transaction`, что и само действие (согласованность — уведомление не может потеряться или появиться без события); `lib/booking/notification-actions.ts` — `markNotificationReadAction` с проверкой владения (нейтральный no-op при чужом id, та же защита от IDOR, что у cancelBookingAction); `app/notifications/page.tsx` — список уведомлений с визуальным выделением непрочитанных; добавлен общий `app/site-header.tsx`, подключённый в `app/layout.tsx` — до этого шага в проекте не было общего header, каждая страница была самостоятельной; header показывает навигацию и счётчик непрочитанных уведомлений на всех страницах, не только на /notifications
  - Результат: гость видит свои бронирования и уведомления в реальных данных, непрочитанные видны сразу в header на любой странице
  - Проверка: debug-эндпоинт (создан и удалён) подтвердил — уведомление создаётся при бронировании и при отмене, привязано к booking, помечается прочитанным, чужой пользователь не может пометить чужое уведомление (IDOR-проверка); `npm run build` проходит; ревью reviewer
  - Итог: ПРИНЯТО (PASS) — жалоба пользователя на отсутствие общего header закрыта реально: SiteHeader подтверждённо подключён единственной точкой в layout.tsx выше {children}, счётчик непрочитанных пересчитывается на каждой странице, транзакционность Booking+Notification и IDOR-защита проверены построчно и практическим прогоном

- [x] 10. Административный контур: бронирования и история
  - Вход: машина состояний Booking описана
  - Действие: `lib/booking/state-machine.ts` — таблица допустимых переходов из specification.md (pending→confirmed/cancelled; confirmed→cancelled/completed/no_show; терминальные никуда); `lib/admin/booking-actions.ts` — единая `transitionBookingStatus` (requireRole(['admin','super_admin']) → проверка допустимости перехода → транзакция: update Booking + create Notification гостю + create AuditLog с actor/action/metadata from-to), обёрнута в 4 именованных action (confirm/cancel/complete/markNoShow); `app/admin/layout.tsx` — единая точка защиты всего контура (`requireRoleOrRedirect(['admin','super_admin'])`), гость получает редирект на `/`; `app/admin/bookings/` — список с фильтрами по статусу/дате (query params) и кнопками действий по текущему статусу; `app/admin/audit-log/` — таблица истории
  - Результат: admin/super_admin управляют потоком бронирований, история фиксируется, гость не может попасть в контур
  - Проверка: debug-эндпоинт (создан и удалён) подтвердил — гость получает ForbiddenError при попытке вызвать admin-action напрямую; admin проходит pending→confirmed→completed с двумя записями AuditLog (актор корректный); повторный confirmed→confirmed и completed→confirmed (терминальный назад) — оба отклонены; гость получил 3 уведомления о статусах; no_show-сценарий подтверждён; доступ гостя к `/admin/bookings` со страницы — редирект на `/`, без сессии — редирект на `/login`; `npm run build` проходит
  - Итог: ПРИНЯТО С ЗАМЕЧАНИЯМИ (PASS WITH NOTES) — ревьюер вручную собрал HMAC-сессию и прогнал реальный HTTP-цикл, нашёл race condition: `booking.status` читался вне транзакции, при двух параллельных admin-действиях на одну бронь обе проверки допустимости проходили по одинаковому устаревшему статусу, оба update применялись — история AuditLog искажалась дублем. Исправлено сразу: `transitionBookingStatus` теперь делает условный `updateMany({where:{id,status:booking.status}})` внутри транзакции — если статус успел смениться параллельным запросом, `count===0` и вся транзакция (Notification+AuditLog) откатывается с понятной ошибкой «уже изменено другим действием». Проверено собственным стресс-тестом: 12 параллельных запросов (6 confirm + 6 cancel) на одну бронь дали ровно 1 успех и ровно 1 запись AuditLog

- [x] 11. Административный контур: столы, окна, роли
  - Вход: шаг 10 готов
  - Действие: допущение (подтверждено пользователем) — «управление правилами доступности окон» из ТЗ реализовано через управление столами, так как временные рамки (14 дней/вт-вс/12:00-21:00/шаг 90 мин) зафиксированы в specification.md как константы без отдельной редактируемой сущности; столы — один из факторов доступности окна («только при наличии подходящего стола»). `lib/admin/table-actions.ts` — create/update/toggleActive с AuditLog на каждое действие; `lib/admin/role-actions.ts` — `changeUserRoleAction`, доступен только `super_admin`, с явными защитами: нельзя менять роль `super_admin`, нельзя менять свою собственную роль, идемпотентная проверка (нельзя назначить уже установленную роль); `app/admin/tables/` — список с инлайн-редактированием и переключателем активности; `app/admin/users/page.tsx` — отдельная защита `requireRoleOrRedirect(['super_admin'])` поверх общего `/admin` layout (admin получает редирект на `/`, не 403/500)
  - Результат: полный административный контур работает по матрице ролей specification.md
  - Проверка: debug-эндпоинты (созданы и удалены) подтвердили — admin не может менять роли (ForbiddenError); super_admin повышает user→admin и понижает обратно, оба перехода в AuditLog; попытка изменить роль другого super_admin и свою собственную — отклонены с понятными сообщениями; admin создаёт/деактивирует стол, гость получает ForbiddenError на попытке; доступ admin к `/admin/users` — редирект на `/`, к `/admin/tables` — 200; `npm run build` проходит
  - Итог: ПРИНЯТО (PASS) — ревьюер лично прогнал реальный HTTP-цикл с сессионными куками seed-аккаунтов (не поверил описанию), подтвердил двухуровневую защиту `/admin/users` (собственный requireRoleOrRedirect поверх общего /admin layout) и все защиты changeUserRoleAction построчно и практически

- [x] 12. Визуальная сборка интерфейса
  - Вход: все экраны функционально готовы (шаги 2-11); три композиционных варианта лендинга сделаны как HTML-артефакты и показаны пользователю, выбран вариант A (классический лендинг: hero + форма отдельным блоком)
  - Действие: шрифты Cormorant Garamond + Manrope подключены через `next/font/google`; CSS-токены design.md заведены в `app/globals.css` через Tailwind v4 `@theme inline` (цвета, radius-card/control) — генерируют utility-классы `bg-primary`, `rounded-card` и т.д.; `SiteHeader`/`SiteHeaderShell` — прозрачный `position: fixed` поверх hero на лендинге, `sticky` непрозрачный на внутренних страницах, тень при скролле; лендинг переведён в стиль варианта A; `/login`, `/register` — карточка форм; `/book` — time-slot кнопки вместо dropdown (из референса Resy/Tock); `/bookings`, `/notifications` — карточки с общим `StatusBadge`; `/admin/*` — таблицы с чередованием строк, вкладки навигации с активным состоянием, кнопки действий по kind (primary/neutral/danger)
  - Результат: интерфейс выглядит как единый продукт Verde Marea на гостевом и административном контуре
  - Проверка: визуальный обход через Playwright-скриншоты (guest/admin/super_admin сессии, 10 экранов) — найден и исправлен баг: header был `sticky` в обычном потоке документа, поэтому прозрачный слой (`bg-text/25`) рисовался поверх светлого фона body, а не поверх зелёного hero (выглядел серым); исправлено на `position: fixed` только для лендинга — header уже правильно накладывается на hero после исправления, подтверждено повторным скриншотом; `npm run build` проходит, ошибок в консоли браузера и логе сервера нет
  - Итог: ПРИНЯТО С ЗАМЕЧАНИЯМИ (PASS WITH NOTES) — ревьюер лично перепроверил header overlay скриншотом (не поверил описанию), все hex/px токены сверены дословно с design.md. Замечание не блокер: на коротком лендинге без сессии `scrollY` едва достигает порога 80px для появления тени — сам механизм исправен (подтверждён на уменьшенном viewport), просто мало контента для скролла на пустой версии страницы

- [ ] 13. Артефакты сдачи и финальная проверка
  - Вход: все функциональные и визуальные шаги готовы
  - Действие: README/runbook (окружение, запуск БД, миграции, seed, вход по ролям, безопасное обновление после смены схемы), чек-лист локальной приёмки, журнал проверок; полный сквозной прогон сценария
  - Результат: проект проходит сценарий запуск → миграции → seed → регистрация → вход → бронирование → уведомления → статус администратора → изменение роли → история изменений
  - Проверка: сквозной ручной прогон по чек-листу приёмки, повторная проверка задетых областей после правок багов
  - Итог: —

## 4. Прямые зависимости и проверки

- `specification.md` — источник правды по сущностям, ролям, правилам
- `design.md` — источник правды по визуальным токенам
- `.env.example` — обязателен, реальный `.env` не коммитится
- `prisma/schema.prisma`, `prisma/migrations` — обязательны для БД
- Проверки: `npm run build`, `npx prisma migrate dev`, `npx prisma db seed`, ручной сквозной сценарий по чек-листу приёмки

## 5. Критерии готовности проекта

- Пройден сценарий: запуск → миграции → seed → регистрация → вход → бронирование → уведомления → статус администратора → изменение роли → история изменений
- Есть `.env.example`, нет реального `.env` в репозитории
- Есть `prisma/schema.prisma` и миграции
- Есть `design.md`, `specification.md`, `Plan.md`, `AGENTS.md`
- Есть README/runbook с инструкциями окружения, запуска БД, миграций, seed, входа по ролям, безопасного обновления после изменений схемы
- Есть чек-лист локальной приёмки и журнал проверок
- Нет внешних интеграций, публичного деплоя, лишнего production-контура
- Нет случайных экспериментальных файлов, мешающих проверить основной сценарий бронирования

## 6. Активное состояние

- Следующий выбранный шаг: `2. Инициализация проекта и окружения`
- Причина выбора: без каркаса Next.js+Prisma+Tailwind невозможно двигаться дальше ни по одному слою
- Текущий фокус: ревью документов этапа 1 (шаг 1) перед стартом кода
