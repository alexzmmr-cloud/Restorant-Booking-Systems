# Plan.md: Verde Marea Booking System — MVP

## 1. Текущий статус

- Приоритет: `P0`
- Текущее состояние: документы этапа 1 составлены, реализация не начата
- Основная цель цикла: рабочая система бронирования столов с локальным запуском, готовая к приёмке по всем 7 этапам курсового ТЗ
- Обязательное ограничение: локальный запуск, без внешних интеграций, без публичного деплоя, без лишнего production-контура
- Основной контекст проекта: `specification.md`, `design.md`
- Секреты: `DATABASE_URL` и любые токены — только в `.env` (не в `.env.example`, не в коммитах, не в ответах ИИ)
- Сейчас в работе: шаг 1 «Легенда, роли и план»

## 1.1 Целевой сценарий

Гость проходит основной маршрут:

1. Регистрируется, входит в систему
2. Выбирает дату, время, количество гостей — видит только допустимые окна
3. Создаёт бронирование (статус `pending`, стол назначается автоматически)
4. Открывает «Мои бронирования» и уведомления
5. Отменяет бронирование сам, если до начала больше 3 часов

Администратор (admin/super_admin) проходит второй маршрут:

1. Открывает административный контур
2. Фильтрует список бронирований по статусу и дате
3. Подтверждает / отменяет / завершает бронирование, отмечает no_show
4. Управляет столами и правилами доступности окон
5. (только super_admin) меняет роль пользователя user ↔ admin
6. Проверяет историю изменений (AuditLog)

## 1.2 Что не входит в текущую версию

- Внешние интеграции: платежи, email/SMS-провайдеры, OAuth/соцсети
- Публичный деплой и production-инфраструктура
- Мобильное приложение
- Мультитенантность (несколько ресторанов)
- Внешние уведомления — только внутренние, в интерфейсе

## 1.3 Допущения и открытые вопросы

### Допущения

- «Внутренние уведомления» — записи в БД, отображаемые в интерфейсе, без внешней рассылки
- Пересечение окон для стола проверяется по точному совпадению даты+времени слота (слоты дискретны, шаг 90 минут)
- Дедлайн отмены — 3 часа от текущего момента до `date`+`time` бронирования
- Деактивация стола не отменяет уже существующие бронирования, только исключает стол из будущего подбора

### Вопросы, которые нельзя восстановить из входа

- Нужна ли пагинация административного списка бронирований при большом объёме — для MVP допущение: без пагинации, с фильтрами
- Поведение при деактивации стола с активным бронированием — для MVP допущение: бронирование сохраняется как есть

## 2. Требования к результату

- Все сущности и правила из `specification.md` реализованы без расхождений
- Роль проверяется на сервере при каждом чувствительном действии, а не только в интерфейсе
- passwordHash никогда не покидает сервер в ответах API
- Правила доступности окон считаются динамически, не хранятся статичным списком
- Визуальная система Verde Marea (`design.md`) применена одинаково в гостевом и административном контуре
- Обязательные артефакты присутствуют: `specification.md`, `Plan.md`, `AGENTS.md`, `design.md`, `.env.example`, `README.md`/runbook, чек-лист приёмки, журнал проверок
- Ничего сверх зафиксированного в specification.md и этом плане не добавляется без обновления обоих документов

## 2.1 Раскладка по слоям

### Frontend
- Next.js App Router, React, TypeScript, Tailwind CSS, Zod
- Формы: регистрация, вход, бронирование (дата/время/гости)
- Личный кабинет гостя: список бронирований, уведомления, кнопка отмены
- Административный контур: список бронирований с фильтрами, управление столами, управление правилами окон, управление ролями (только super_admin), просмотр истории изменений
- Общие UI-токены и компоненты по `design.md` (кнопки, карточки, бейджи статусов, таблицы, формы, состояния loading/empty/error)

### Backend
- Next.js route handlers / server actions, TypeScript, Zod (те же схемы валидации, что и на клиенте)
- Сессии — упрощённый серверный механизм (без внешних auth-провайдеров)
- Проверка роли на сервере перед каждым чувствительным действием
- bcryptjs: `hash(password, 12)` при регистрации, `compare(inputPassword, user.passwordHash)` при входе
- Логика расчёта доступных окон, подбора стола, переходов статусов, проверки дублей — на сервере

### Database
- `User`: id, email, passwordHash, name, role, createdAt, updatedAt
- `Table`: id, name, capacity, isActive
- `Booking`: id, userId, tableId, date, time, guestsCount, status, createdAt, updatedAt
- `Notification`: id, userId, bookingId, message, isRead, createdAt
- `AuditLog`: id, actorId, action, targetType, targetId, metadata, createdAt

- роли (`User.role`): `user`, `admin`, `super_admin`
- статусы (`Booking.status`): `pending`, `confirmed`, `cancelled`, `completed`, `no_show`

- связи: `User 1—N Booking`, `User 1—N Notification`, `Table 1—N Booking`, `User 1—N AuditLog` (как actor)

- правило доступности окна: ближайшие 14 дней, вторник–воскресенье, шаг 90 минут, диапазон 12:00–21:00, без прошедшего времени, без занятых окон, только при наличии подходящего свободного стола
- правило подбора стола: минимально подходящая вместимость среди свободных на выбранное окно
- правило дублей: один пользователь — не больше одного активного (`pending`/`confirmed`) бронирования на дату+время; один стол — не больше одного бронирования на дату+время
- правило отмены: гость отменяет сам не позднее чем за 3 часа до `date`+`time`; после дедлайна — только admin/super_admin
- правило ролей: admin не может менять роль `super_admin` и не может назначать/снимать `admin`; только `super_admin` меняет `user` ↔ `admin`

## 3. Что делаем по шагам

- [x] 1. Легенда, роли и план
  - Вход: ТЗ курса, палитра и шрифты бренда от пользователя
  - Действие: собраны `specification.md`, `design.md`, `Plan.md`, `AGENTS.md`
  - Результат: зафиксированы легенда, MVP-граница, роли, сценарии, визуальное направление, план по слоям
  - Проверка: ревью через субагента reviewer
  - Итог: документы этапа 1 составлены, ждут ревью перед стартом реализации

- [x] 2. Инициализация проекта и окружения
  - Вход: `specification.md`, `Plan.md`, `design.md` готовы
  - Действие: `create-next-app` с TypeScript+Tailwind, установка Prisma 7 (postgresql), @prisma/client, Zod, bcryptjs, dotenv с точной фиксацией версий; `.env.example` создан и коммитится, реальный `.env` в `.gitignore`
  - Результат: Next.js проект собирается, Prisma-схема валидна, наши документы не задеты генератором
  - Проверка: `npm run build` — чисто, `npx prisma validate` — схема валидна, ревью reviewer
  - Итог: ПРИНЯТО (PASS) — сборка и схема валидны, версии зафиксированы, `.env`/`.env.example` разведены корректно

- [x] 3. Схема данных и миграции
  - Вход: сущности из specification.md/Plan.md раздела Database
  - Действие: `prisma/schema.prisma` с моделями User, Table, Booking, Notification, AuditLog, enum'ами Role и BookingStatus; создана роль/база PostgreSQL `verde_marea`; применена первая миграция `init`
  - Результат: локальная PostgreSQL база разворачивается миграцией без ручных правок, Prisma Client сгенерирован, `npm run build` проходит
  - Проверка: `npx prisma migrate dev` применился чисто на пустой БД, `\dt` показывает все 5 таблиц, ревью reviewer
  - Итог: ПРИНЯТО (PASS) — схема соответствует specification.md без расхождений; первая итерация ревью нашла реальные креды БД в `.env.example`, исправлено плейсхолдером и сменой пароля роли, повторное ревью подтвердило PASS

- [x] 4. Seed и контрольные данные
  - Вход: применённая схема
  - Действие: `prisma/seed.ts` создаёт super_admin, admin, двух гостей (пароль у всех `Password123!`, хеш через bcryptjs 12 rounds) и 6 столов вместимостью 2/2/4/4/6/8; добавлен `lib/prisma.ts` — общий Prisma Client с driver adapter `@prisma/adapter-pg` (обязателен в Prisma 7); добавлено `@unique` на `Table.name`, пересоздана чистая init-миграция (БД была пустой, публикации ещё не было)
  - Результат: `npx prisma db seed` создаёт контрольные данные, повторный запуск не плодит дубли (upsert по email/name)
  - Проверка: `npx prisma db seed` дважды подряд — количество записей не меняется (4 User, 6 Table); `npm run build` проходит; ревью reviewer
  - Итог: ПРИНЯТО (PASS) — seed соответствует specification.md, bcryptjs 12 rounds подтверждён, идемпотентность и сборка проверены практическими прогонами

- [x] 5. Регистрация, вход, сессия
  - Вход: модель User готова
  - Действие: `lib/auth/session.ts` — подписанная HMAC httpOnly-кука (userId + подпись на `SESSION_SECRET`, timing-safe сравнение); `lib/auth/actions.ts` — server actions `registerAction`/`loginAction`/`logoutAction` с bcryptjs.hash(password,12)/compare; `lib/auth/current-user.ts` — роль читается из БД по userId при каждом запросе (не хранится в куке, чтобы смена роли super_admin применялась мгновенно); `lib/auth/require-role.ts` — заготовка проверки роли для будущих защищённых маршрутов; страницы `/register`, `/login`, обновлена `/`
  - Результат: пользователь регистрируется и входит, сессия переживает перезагрузку страницы, роль всегда актуальна из БД
  - Проверка: временный debug-эндпоинт прогнал сценарии (регистрация → bcrypt-хеш корректного формата → сессия установлена → дубль email отклонён → верный пароль входит → неверный пароль отклонён → несуществующий email отклонён с тем же сообщением, что и неверный пароль → прямой bcryptjs.compare подтверждает хеш), затем эндпоинт удалён; `npm run build` проходит; ревью reviewer
  - Итог: ПРИНЯТО (PASS) — bcryptjs 12 rounds дословно по specification.md, passwordHash нигде не покидает сервер (подтверждено grep), защита от user enumeration на месте

- [ ] 6. Серверная проверка ролей
  - Вход: сессии работают
  - Действие: middleware/серверные проверки роли на всех чувствительных маршрутах согласно матрице ролей из specification.md
  - Результат: запрещённые действия блокируются на сервере независимо от интерфейса
  - Проверка: прямой запрос к административному эндпоинту от имени user отклоняется
  - Итог: —

- [ ] 7. Расчёт доступных окон и подбор стола
  - Вход: модели Table, Booking готовы
  - Действие: серверная функция расчёта доступных слотов по правилам (14 дней, вт–вс, шаг 90 мин, 12:00–21:00, без прошлого, без занятых, только с подходящим столом) и подбора стола по вместимости
  - Результат: список доступных окон на дату всегда соответствует правилам и текущим бронированиям
  - Проверка: ручной сценарий — забронировать все столы нужной вместимости на слот, убедиться что слот пропадает из списка
  - Итог: —

- [ ] 8. Создание и отмена бронирования гостем
  - Вход: расчёт окон и подбор стола готовы
  - Действие: форма бронирования (дата/время/гости), создание Booking в статусе pending с проверкой дублей пользователя и стола, кнопка самостоятельной отмены с проверкой дедлайна 3 часа
  - Результат: гость создаёт и отменяет бронирования по правилам specification.md
  - Проверка: попытка создать второе активное бронирование на то же время отклоняется; отмена после дедлайна отклоняется
  - Итог: —

- [ ] 9. Личный кабинет гостя и уведомления
  - Вход: бронирования создаются
  - Действие: страница «Мои бронирования», создание Notification при значимых событиях (создание/подтверждение/отмена), страница уведомлений
  - Результат: гость видит свои бронирования и уведомления в реальных данных
  - Проверка: создать бронирование → появляется в списке и генерируется уведомление
  - Итог: —

- [ ] 10. Административный контур: бронирования и история
  - Вход: машина состояний Booking описана
  - Действие: список бронирований с фильтрами по статусу/дате, действия подтверждения/отмены/завершения/no_show с проверкой допустимых переходов, запись в AuditLog при каждом действии
  - Результат: admin/super_admin управляют потоком бронирований, история фиксируется
  - Проверка: смена статуса создаёт запись AuditLog с корректным actor/action; попытка перевести терминальный статус обратно в активный отклоняется
  - Итог: —

- [ ] 11. Административный контур: столы, окна, роли
  - Вход: шаг 10 готов
  - Действие: управление столами (создание/редактирование/деактивация), управление правилами доступности окон, страница смены роли user↔admin только для super_admin с записью в AuditLog
  - Результат: полный административный контур работает по матрице ролей
  - Проверка: попытка admin сменить роль другого пользователя отклоняется; super_admin повышает user до admin и понижает обратно
  - Итог: —

- [ ] 12. Визуальная сборка интерфейса
  - Вход: все экраны функционально готовы
  - Действие: применение токенов `design.md` (палитра, шрифты, отступы, кнопки, бейджи статусов, состояния loading/empty/error) во всех экранах гостевого и административного контура
  - Результат: интерфейс выглядит как единый продукт Verde Marea
  - Проверка: визуальный обход всех основных экранов на соответствие design.md
  - Итог: —

- [ ] 13. Артефакты сдачи и финальная проверка
  - Вход: все функциональные и визуальные шаги готовы
  - Действие: README/runbook (окружение, запуск БД, миграции, seed, вход по ролям, безопасное обновление после смены схемы), чек-лист локальной приёмки, журнал проверок; полный сквозной прогон сценария
  - Результат: проект проходит сценарий запуск → миграции → seed → регистрация → вход → бронирование → уведомления → статус администратора → изменение роли → история изменений
  - Проверка: сквозной ручной прогон по чек-листу приёмки, повторная проверка задетых областей после правок багов
  - Итог: —

## 4. Прямые зависимости и проверки

- `specification.md` — источник правды по сущностям, ролям, правилам
- `design.md` — источник правды по визуальным токенам
- `.env.example` — обязателен, реальный `.env` не коммитится
- `prisma/schema.prisma`, `prisma/migrations` — обязательны для БД
- Проверки: `npm run build`, `npx prisma migrate dev`, `npx prisma db seed`, ручной сквозной сценарий по чек-листу приёмки

## 5. Критерии готовности проекта

- Пройден сценарий: запуск → миграции → seed → регистрация → вход → бронирование → уведомления → статус администратора → изменение роли → история изменений
- Есть `.env.example`, нет реального `.env` в репозитории
- Есть `prisma/schema.prisma` и миграции
- Есть `design.md`, `specification.md`, `Plan.md`, `AGENTS.md`
- Есть README/runbook с инструкциями окружения, запуска БД, миграций, seed, входа по ролям, безопасного обновления после изменений схемы
- Есть чек-лист локальной приёмки и журнал проверок
- Нет внешних интеграций, публичного деплоя, лишнего production-контура
- Нет случайных экспериментальных файлов, мешающих проверить основной сценарий бронирования

## 6. Активное состояние

- Следующий выбранный шаг: `2. Инициализация проекта и окружения`
- Причина выбора: без каркаса Next.js+Prisma+Tailwind невозможно двигаться дальше ни по одному слою
- Текущий фокус: ревью документов этапа 1 (шаг 1) перед стартом кода
